# Docker Compose interpolates "$" inside this file: write a literal $ as $$.
SECRET_KEY=change-me

DEBUG=False
# Comma-separated hostnames; * allows any.
ALLOWED_HOSTS=*

POSTGRES_DB=flippedclassroom
POSTGRES_USER=flippedclassroom
POSTGRES_PASSWORD=change-me

EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_HOST_USER=noreply@example.com
EMAIL_HOST_PASSWORD=change-me
